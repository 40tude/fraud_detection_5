[build]
# Avoid OneDrive sync issues by redirecting build artifacts
target-dir = "C:/Users/phili/rust_builds/Documents/Programmation/rust/13_fraud_detection_5"

[target.x86_64-pc-windows-msvc]
# Enable native CPU optimizations (SSE4.2, AVX2, etc.) on the dev machine
# rustc --print cfg -C target-cpu=native | Select-String "target_feature"
# Get-CimInstance -ClassName Win32_Processor | Select-Object Name, Caption, NumberOfCores, NumberOfLogicalProcessors
rustflags = ["-C", "target-cpu=native"]
